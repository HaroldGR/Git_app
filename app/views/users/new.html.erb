<%= form_for :user, :url=>{:action => 'create'} do |f|  %>
<%= render :partial =>'form', :locals =>{:f=>f }  %>
  <div class="actions">
    <%= f.submit "Ingresar " %>
  </div>
<%  end %>
<%= link_to 'Volver', :action =>'index' %>
